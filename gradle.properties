# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# We no longer need to auto-convert third-party libraries to use AndroidX, which slowed down the build
android.enableJetifier=false

# Allow Gradle to use up to 8 GB of RAM
org.gradle.jvmargs=-Xmx8192M

# Allow Gradle to execute in parallel
org.gradle.parallel=true

# Allow Gradle deamon to run in the background
org.gradle.daemon=true

# Allow gradle to skip the gradle configuration step if nothing has changed
org.gradle.configuration-cache=true

# Allow gradle to cache parts of the build that have already been completed
org.gradle.caching=true